# DeviceRide Service

Pseudo Declaration

```
DeviceService <Singleton>
    startRide()
    stopRide()

    pause()
    resume()

    check(capability|adapter)
  

    onDeviceData(adapter, data:DeviceData)
```
